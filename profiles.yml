sandbox_lightdash:
  target: dev
  outputs:
    dev:
      type: postgres
      host: localhost
      port: 5432
      user: lightdash
      password: lightdash_password
      dbname: lightdash
      schema: public
      threads: 1
      sslmode: disable
